# bruno.nvim (local fork)

This is a vendored, locally-modified copy of `romek-codes/bruno.nvim` so the config can iterate on it without depending on upstream changes.

- Upstream: https://github.com/romek-codes/bruno.nvim

